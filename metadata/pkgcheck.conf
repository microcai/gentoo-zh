[DEFAULT]
# Disable noisy keywords (checks)
checks = -RedundantVersionCheck
# gentoo-zh keeps packages unstable, so PotentialStable is policy noise.
keywords = -NonsolvableDepsInDev,-PotentialStable
exit = NonexistentDeps
