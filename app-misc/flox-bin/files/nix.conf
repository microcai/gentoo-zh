# Flox defaults live in flox.conf. Keep site-local overrides in this file.
include flox.conf

# Gentoo does not create the upstream nixbld user pool for this binary bundle.
# Leaving this empty keeps Nix usable without extra account packages, but
# daemon access must stay limited because builds run as the daemon user.
build-users-group =
allowed-users = root @wheel
