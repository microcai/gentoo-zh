diff --git a/Cargo.toml b/Cargo.toml
index 0e6a3d0..29e2dda 100644
--- a/Cargo.toml
+++ b/Cargo.toml
@@ -39,6 +39,8 @@ panic = "abort"
 
 
 [dependencies]
+# openssl https://github.com/ogham/dog/issues/122
+openssl = { version = "0.10", features = ["vendored"] }
 
 # dns stuff
 dns = { path = "./dns" }
